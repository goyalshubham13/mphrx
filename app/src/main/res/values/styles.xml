<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/greyColor</item>
        <item name="colorPrimaryDark">@color/greyColor</item>
        <item name="colorAccent">@color/greyColor</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BlueBtn">
        <item name="android:textColor">@color/blue_btn_txt_color</item>
        <item name="android:background">@drawable/blue_action_btn</item>
        <item name="android:textSize">16dp</item>
    </style>
    <style name="ListDesc">
        <item name="android:textSize">16sp</item>
    </style>
    <style name="DetailRow2">
        <item name="android:textSize">16sp</item>
    </style>
</resources>
